pipeline {
  agent { label 'jenkins-ssh-slave' }
  stages {
    stage('kafka-installation') {
      steps {
        sh 'ansible-playbook -i inventory KafkaLogstash-Playbooks-main/kafka.yml'
      }
    }
  }
}
